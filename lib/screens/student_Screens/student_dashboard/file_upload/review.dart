import 'package:flutter/material.dart';

class Upload_Review extends StatelessWidget {
  const Upload_Review({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: <Widget>[
        Text("TEXT"),
      ],
    );
  }
}
